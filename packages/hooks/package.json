{
  "name": "@microfrontend-nextjs/hooks",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "dependencies": {
    "react": "^17.0.2"
  },
  "scripts": {
    "dev": "yarn run clean && yarn run watch",
    "build": "yarn run clean && yarn run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/react": "^17.0.24",
    "tsc": "^2.0.3",
    "typescript": "^4.4.3"
  }
}
